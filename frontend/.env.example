# Copy to .env.local for local dev (gitignored via *.local).
# In Vercel: set VITE_API_BASE_URL as a project env var pointing at the
# deployed Railway backend URL, e.g. https://your-app.up.railway.app
VITE_API_BASE_URL=http://localhost:8000
